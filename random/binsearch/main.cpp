#include <bits/stdc++.h>

using namespace std;
// x    0123456789...
// f(x) FFFFFFTTTT...
// Task search for 5:
// -1 if all T

// Say we want to binary search for 6:
// keep invariant in mind: [lo, hi] always contains 6
vector<int> v = {0, 1};
int lb(int l, int r, int val) {}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    // cout << lower_bound(0, v.size(), -2) << endl;
}