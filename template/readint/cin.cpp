#include <bits/stdc++.h>

using namespace std;
signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int N;
    cin >> N;
    int x;
    for (int i = 0; i < N; i++) {
        cin >> x;
        cout << x << endl;
    }
    cout << x << endl;
}