#include <bits/stdc++.h>

using namespace std;

string S;
string T;
int l = 0, r = 0;
signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> S >> T;
    int ans = 0;
    bool offset = 0;
    for (int i = 0; i < T.size(); i++) {
    }
    cout << ans << endl;
}