#include <bits/stdc++.h>

using namespace std;

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout << 1 << endl;
    int x;
    cin >> x;
    if (x == 2) {
        cout << 3 << endl;
    } else {
        cout << 2 << endl;
    }
}